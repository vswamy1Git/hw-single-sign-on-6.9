%h1 Listing profiles

%table
  %thead
    %tr
      %th Primary language
      %th Secondary language
      %th Favorite movie
      %th Worst movie
      %th Favorite genre
      %th User
      %th
      %th
      %th

  %tbody
    - @profiles.each do |profile|
      %tr
        %td= profile.primary_language
        %td= profile.secondary_language
        %td= profile.favorite_movie
        %td= profile.worst_movie
        %td= profile.favorite_genre
        %td= profile.user
        %td= link_to 'Show', profile
        %td= link_to 'Edit', edit_profile_path(profile)
        %td= link_to 'Destroy', profile, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Profile', new_profile_path
